services:
  frontend:
    build: ./frontend
    ports:
      - "3001:3000"
    depends_on:
      - server

  server:
    build: ./server
    ports:
      - "5001:5000"
    env_file:
      - ./server/.env
